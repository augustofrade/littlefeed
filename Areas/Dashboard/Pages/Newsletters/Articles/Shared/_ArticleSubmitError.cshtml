@model List<string>

<div class="alert alert-danger mb-5" role="alert">
    Error submitting article.
    @if(Model.Count > 0) {
        <ul>
        @foreach (var error in Model)
        {
            <li>@error</li>
        }
        </ul>
    }
</div>
